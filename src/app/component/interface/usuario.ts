export interface product {
  id?: string;
  stock: number;
  ventas: number;
  fecha: number;
  producto: string;
}
export interface prodcutUpdate {
  id?: string;
  stock: number;
  ventas: number;
  fecha: number;
  producto: string;
}
